2026-03-16: Centralized client identity cache in `apps/web/src/lib/auth/client-user-cache.ts`; `AuthSyncProvider` now owns all writes/clears, while storage and retention helpers should depend on `getClientUserIdentity()` instead of parsing `edunexus_current_user` inline.
2026-03-16: Removing whole feature slices under `apps/web/src/app`, `apps/web/src/components`, and `apps/web/src/lib` also requires pruning unused schema fields in `apps/web/src/lib/server/store.ts`; otherwise typecheck/build fail on orphaned imports even after routes are deleted.
2026-03-16: Next 16 prerendering requires auth pages that call `useSearchParams()` to render inside `<Suspense>` boundaries; wrapping `apps/web/src/app/(auth)/login/page.tsx` and `apps/web/src/app/(auth)/register/page.tsx` restored build verification during cleanup work.
2026-03-16: After deleting an App Router route, `apps/web/.next/types/validator.ts` can keep stale references to the removed page until `apps/web/.next` is cleared; cleaning the generated directory before verification avoids false-negative `tsc --noEmit` failures during cleanup.
2026-03-16: KB ownership boundaries are easiest to regression-test by mocking `@/lib/server/auth-utils` and `@/lib/server/document-service` at the route layer; this directly proves `/api/kb/docs` and `/api/kb/doc/[id]` derive scope from the session and ignore client-supplied `userId` values.
2026-03-16: `prisma/schema.prisma` did not have an explicit `Document.authorId` index; adding `@@index([authorId])` gives a verifiable ownership filter path for `findMany/findFirst` queries keyed by `authorId`.
2026-03-16: `apps/web/src/lib/server/store.ts` should aggressively normalize parsed JSON (`Array.isArray`/record guards) and ignore unknown legacy keys; this keeps session/plan APIs stable even when historical `db.json` still contains removed fields like `resources`/`users`.
2026-03-16: Auth/isolation regression coverage now lives at the route boundary: `apps/web/src/auth.test.ts` for public-vs-protected auth contract, `apps/web/src/app/api/kb/ownership-boundary.test.ts` and `apps/web/src/app/api/workspace/workspace-api.test.ts` for unauthenticated `401` plus cross-user isolation, and `apps/web/src/app/api/removed-surfaces.test.ts` for deleted API surfaces asserted absent from the filesystem.
2026-03-16: `apps/web/src/lib/hooks/__tests__/use-auto-save.test.ts` needs a jsdom test environment because `@testing-library/react`'s hook renderer depends on `document`; keeping the file-scoped `// @vitest-environment jsdom` directive plus `jsdom` in `apps/web/package.json` makes mixed node/jsdom Vitest suites pass without switching the whole project away from the default node environment.
